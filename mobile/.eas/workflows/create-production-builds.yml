name: Create Production Builds

on:
  push:
    branches:
      - production-builds

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: production-apk
  build_ios:
    type: build
    params:
      platform: ios
      profile: production
